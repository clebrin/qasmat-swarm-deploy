all:
  children:
  # Provide IP adresses
    _master:
      hosts:
        proxy:
          ansible_host: 
    _worker:
      hosts:
        storage1:
          ansible_host: 
        storage2:
          ansible_host:
        storage3:
          ansible_host: 
        web:
          ansible_host: 
  vars:
    ansible_user: 
    ansible_port: 22
    github:
      # In case you got access to private images; leave empty for public images
      token:
      username:
    # Storage names must be differents
    storage1: 
    storage2: 
    storage3: 
    # Domain names for the webapp, authentication and qasmat's api e.g. domain.com 
    # and subdomain.domain.com for web_dns/auth_dns/api_dns
    web_dns: 
    auth_dns: 
    api_dns: 
    domain: 
    docker_images:
      repository: # TODO : provided by VeriQloud
      web:
        name: # TODO : provided by VeriQloud
        tag: # TODO : provided by VeriQloud
      storage:
        name: # TODO : provided by VeriQloud
        tag: # TODO : provided by VeriQloud
      proxy:
        name: # TODO : provided by VeriQloud
        tag: # TODO : provided by VeriQloud
